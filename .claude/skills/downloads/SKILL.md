---
name: downloads
description: "Use when working on offline downloads: saving tracks or albums for offline listening, the /downloads page, the Cache API buckets stablekraft-downloads-v1 and stablekraft-downloads-art-v1, the StableKraftDownloadsDB IndexedDB, the DownloadManager and reference counting, cover art offline, downloads disappearing after an update or deploy, manual Offline mode, or the sk_offline_mode toggle."
---

# downloads

Offline downloads. The three storage identifiers are load-bearing — renaming one orphans every user’s downloads.

## Tests for this subsystem

```
npm run test:downloads                              # lib/downloads/*.test.ts
```

---

## Offline Downloads (`lib/downloads/*`, `contexts/DownloadsContext.tsx`)
Save tracks/albums for offline listening. Works in the PWA **and** the native Capacitor app (its WebView loads the live site) from one **web deploy** — no native plugin/APK. Cache API bytes + sibling IndexedDB metadata + a reference-counting `DownloadManager` (module singleton).

- **Storage split**: audio bytes in Cache API bucket `stablekraft-downloads-v1` (fetched via `getProxiedAudioUrl` to dodge CORS, keyed by `primaryPlaybackKey`); cover images in `stablekraft-downloads-art-v1` (fetched via `/api/proxy-image` so bytes are same-origin/readable → blob URL); metadata in a **separate** IndexedDB `StableKraftDownloadsDB` (isolated from the hot `StableKraftDB` to avoid version-bump risk). Storage backend is an injectable `DownloadsBackend` so `download-manager.ts` is unit-tested without a browser: **`npm run test:downloads`** (`tsx --test`, no jest/vitest in repo).
- **Persistence across updates (do NOT break — downloads target limited-bandwidth users)**: downloads live entirely client-side (Cache API + IndexedDB, origin-scoped), so a **web deploy never touches them** (next-pwa's SW only manages its own `pages-cache`/`static-*`/precache buckets; the only code that deletes the download buckets is the explicit user `clearAllDownloads()`). A **zapstore APK update preserves them** too — Android keeps app data across an in-place update **as long as the APK is signed with the same keystore** (`~/keystores/stablekraft-release.jks`; a key change forces uninstall→reinstall = wipe). The three storage identifiers (`stablekraft-downloads-v1`, `stablekraft-downloads-art-v1`, `StableKraftDownloadsDB`) are **load-bearing — never rename them**, and bump `downloads-db.ts` `DB_VERSION` only with an **additive** `onupgradeneeded` migration (never `deleteObjectStore`/recreate `downloads`). Renaming a bucket or a destructive migration would orphan every download and force a re-download over the user's data plan. Guard comments live at all three declaration sites.
- **Reference counting**: a track saved via an album AND individually shares ONE cached copy; bytes (and the album cover) evict only when the LAST owner is removed. Owner = `'track'` or `` `album:${feedId ?? id}` ``. `downloadOne` dedups concurrent fetches — same-key callers join one fetch via the `inFlight` map (owner Set + shared `AbortController`); the fetch aborts only when the **last** waiting owner cancels. Do NOT collapse to a single-owner controller (strands a joined owner). The `AbortController` is created **before** `acquire()` so a cancel while `queued` is honored. `removeByKey` is an intentional force-delete for the /downloads manage-storage page (ignores ref owners — documented in-code).
- **Playback** (`contexts/AudioContext.tsx`): `getAudioUrlsToTry` prepends a same-origin `blob:` URL for a downloaded track; the network stream stays as fallback (self-heals if bytes were evicted via `forgetEvicted`). Purely additive to the Android ping-pong / gapless machinery — do not disturb.
- **UI — download is decoupled from the favorite heart** (`components/downloads/DownloadButton.tsx`): the heart is a pure two-state favorite; a separate `DownloadButton` mounts beside it on album detail (album + per-track), `AlbumCard`, `NowPlayingScreen`. Gated on a resolvable media URL **and** `DownloadsProvider` presence (`useDownloadsSafe` → renders null outside the provider), and excludes VTS/playlist/publisher targets. Do NOT re-merge download into the heart (a tri-state heart traps the favorite: no one-tap unfavorite, dead offline).
- **A listing page must NOT be made to ship track lists so this button can pick an icon.** `getAlbumState(album)` needs the tracks, and `AlbumCard` calls it every render — which is why `/api/favorites/albums` used to include up to 50 rows per favorited feed: 593,191 bytes of a 1.27 MB page load, 1,007 rows, none of them rendered. Use **`getAlbumStateByOwner(feedId, totalHint)`** instead: every `DownloadRecord` names its owners and an album owner is `album:<feedId>`, so the manager answers from its own index. Callers that DO hold their tracks keep the exact path (album detail, Now Playing, the home grid via `albums-fast`) — only a caller that omits them falls back.
  - `total` resolves in three steps and the order matters: the in-memory `albumKeys` set while an album is downloading (in-flight tracks have no record yet, so without it a fresh album jumps from idle to done), then **`albumTotal`** on the records (without it an album interrupted at 3 of 10 has 3 records, all downloaded, and reads as complete), then the record count for records written before that field. `albumTotal` is a new property on an existing store — **no `DB_VERSION` bump, no migration**.
  - The server sends `downloadableTrackCount` and `hasVideoTracks` in place of the rows, from one grouped query. That count **mirrors `isDownloadable` only as far as SQL can** — `isNonDownloadableUrl` (HLS) cannot be expressed there, so it can over-count. `DownloadButton` therefore toasts when an album resolves to zero downloadable tracks rather than sitting idle, which is the same failure the original `!!url` gate caused.
  - Downloading a card with no track list fetches them on the click (`withResolvedTracks` in `DownloadsContext`, via `/api/tracks?feedId=`). Skip that and the Downloads page lists rows called "Unknown".
- **`/downloads` page** (`app/downloads/DownloadsClient.tsx`): collapsible albums (default collapsed — a large library stays a short list), **album-order** playback (`trackOrder` captured at download time since downloads finish in arbitrary order), now-playing highlight (matched by `primaryPlaybackKey`), per-album **owner-scoped** delete + confirm, storage-usage bar, back button. (The Offline-mode switch is **not** here any more — see below.)
- **Cover art offline**: `downloadManager.getCoverObjectUrl` → `getCoverUrl` (context) resolves a cached cover to a blob URL on the /downloads page AND in Now Playing, falling back to the network URL. Cached once per album, evicted with the album's last track. Blob object URLs are revoked on change/unmount — keep that lifecycle.
- **Manual Offline mode** (`DownloadsContext` + `AudioContext`): `isOnline` is driven **solely** by a persisted user toggle (`localStorage['sk_offline_mode']`) — the app deliberately does NOT react to real `navigator.onLine` (connection loss must never force offline UI or redirect; playback prefers the downloaded blob regardless). Do not re-add `useOnlineStatus` reactivity. **It's a QoS switch** ("going into a poor-signal area"): when on, (1) new downloads are blocked, and (2) `AudioContext` refuses to *attempt* streaming a non-downloaded track — `blockNonDownloadedInOfflineMode()` (reads `sk_offline_mode` directly, awaits `downloadManager.init()`) gates all three play entry points (`playAlbum`/`playShuffledTrack`/`playTrack`) so a flaky connection can't hang playback; downloaded tracks play normally. It is NOT a hard network block (deliberately — see the declined "block all network" option; browsing still uses the network, falling back to the localStorage `cachedAlbums` + SW-cached shell on a poor connection).
  - **The switch lives in Settings → Data & Offline** (`components/Settings/DataSettings.tsx`), beside Data Saver. It was on the /downloads page until 2026-09-20 and moved because the app's two manual "use less network" switches sat in two different places, so nobody looking for one found the other. Same key, same `DownloadsContext`, same behaviour — only where it is drawn changed, and anyone who had it on still has it on. Data Saver's own contract is in CLAUDE.md; the two are independent and compose.
- **Entry points**: `/downloads` linked from the account menu (`UserMenu`) and a home-header button (desktop + mobile); `/downloads` added to next-pwa precache (`additionalManifestEntries`) so the offline page's CTA reaches it offline.
